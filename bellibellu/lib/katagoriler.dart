import 'package:flutter/material.dart';




class Katagoriler extends StatelessWidget {
  const Katagoriler({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('katagoriler'),
    );
  }
}